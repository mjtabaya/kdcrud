<% provide(:title, "Home") %>
<div class="container">

  <div class="center jumbotron">
    <h1>User CRUD</h1>
    <ul>
      <li>Create a tool that will allow users to register and login.</li>
      <li>Every user contains a first name, last name, phone number, credit card number, email and password.</li>
      <li>Users with "publisher" role that have CRUD access on Articles.</li>
      <% if can? :manage, User %>
      	<li>Visible only for superadmins and supervisors! </li>
      <% end %>
    </ul>
    <h1>
      <% if current_user.nil? %>
      	<%= link_to new_user_session_path, class: 'login-button' do %>Sign in<% end %>
      <% else %>
      	<%= link_to app_dashboard_index_path, class: 'login-button' do %>Go to App<% end %>
      <% end %>
    </div>
  </h1>
  </div>

</div>
